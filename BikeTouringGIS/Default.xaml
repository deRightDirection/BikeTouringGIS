<Metro:MetroWindow  x:Class="BikeTouringGIS.Default"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:Fluent="urn:fluent-ribbon"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:BikeTouringGIS.ViewModels"
        xmlns:local="clr-namespace:BicycleTripsPreparationApp"
                    xmlns:project="clr-namespace:BikeTouringGIS"
        mc:Ignorable="d"
                    xmlns:System="clr-namespace:System;assembly=mscorlib"
                    Icon="bikecons_touring-32x32.png"
		Title="Bike Touring GIS" 
                     Background="White"
                    BorderBrush="DarkGreen"
                    BorderThickness="1"
                    GlowBrush="LimeGreen"
             d:DesignHeight="1000" d:DesignWidth="1000"
    xmlns:Metro="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro" WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <Metro:MetroWindow.DataContext>
        <vm:BikeTouringGISViewModel/>
    </Metro:MetroWindow.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <project:MainMenu Grid.Row="0" Grid.ColumnSpan="2" x:Name="mainmenu"/>
        <project:MainScreen Grid.Row="1" Grid.ColumnSpan="2"/>
        <project:LayerList Grid.Row="2" Grid.Column="0" Margin="5 0 1 5" Map="{Binding Map, ElementName=biketouringgismap}" Menu="{Binding ElementName=mainmenu}"/>
        <project:BikeTouringGISMap Grid.Row="2" Grid.Column="1" Margin="1 0 5 5" x:Name="biketouringgismap" Menu="{Binding ElementName=mainmenu}"/>
        <project:StatusBar Grid.Row="3" Grid.ColumnSpan="2" MapView="{Binding MapView, ElementName=biketouringgismap}"/>
        <project:UrlBar Grid.Row="4" Grid.ColumnSpan="2" Margin="5"/>
    </Grid>
</Metro:MetroWindow>
